#include <stdio.h>
#include <stdlib.h>
/**
 * main - entry point
 * Description: number from 0 to 10
 * Return: 0 (success)
*/
int main(void)
{
int digit = 0;
{
while
(digit <= 9);
printf("%d", digit);
digit++;
}
printf('\n);
return (0);
}
